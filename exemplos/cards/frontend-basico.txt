# Cards Prontos - Frontend Básico

*Copie e cole estes cards no seu Anki*

## 📋 Como Usar
1. Copie o texto entre as linhas
2. No Anki: File → Import
3. Cole o texto ou salve como arquivo .txt
4. Configure: Front = Campo 1, Back = Campo 2, Tags = Campo 3

---

## 💻 JavaScript - Conceitos Básicos

```
O que é uma variável em JavaScript?	Um container para armazenar dados que podem ser alterados durante a execução	javascript conceito variaveis

O que é hoisting em JavaScript?	Comportamento onde declarações de var e function são "movidas" para o topo do escopo antes da execução	javascript conceito hoisting

Qual a diferença entre null e undefined?	undefined = variável declarada mas sem valor; null = valor "vazio" intencionalmente atribuído	javascript diferenca tipos

O que é closure em JavaScript?	Função que tem acesso ao escopo da função pai, mesmo após a função pai ter terminado de executar	javascript conceito closure funcao

O que significa "JavaScript é dinamicamente tipado"?	O tipo da variável é determinado em tempo de execução, não na declaração	javascript conceito tipos
```

## 🔧 JavaScript - Sintaxe Essencial

```
Como declarar uma variável em JavaScript?	let nomeVariavel = valor;	javascript sintaxe variaveis

Como declarar uma constante em JavaScript?	const nomeConstante = valor;	javascript sintaxe variaveis

Como criar uma função arrow em JavaScript?	const nomeFuncao = (parametros) => { return resultado; }	javascript sintaxe funcao arrow

Como criar um array vazio em JavaScript?	const array = []; ou const array = new Array();	javascript sintaxe array

Como acessar uma propriedade de objeto em JavaScript?	objeto.propriedade ou objeto['propriedade']	javascript sintaxe objeto

Como desestruturar um array em JavaScript?	const [primeiro, segundo] = array;	javascript sintaxe destructuring array

Como desestruturar um objeto em JavaScript?	const {nome, idade} = objeto;	javascript sintaxe destructuring objeto
```

## 📊 JavaScript - Métodos de Array

```
Qual método retorna um novo array com elementos transformados?	map() - executa função em cada elemento e retorna novo array	javascript metodo array map

Como filtrar elementos de um array?	array.filter(elemento => condicao) - retorna novo array com elementos que passam no teste	javascript metodo array filter

Como somar todos os valores de um array numérico?	array.reduce((acc, valor) => acc + valor, 0)	javascript metodo array reduce

Como encontrar um elemento específico em array?	array.find(elemento => condicao) - retorna primeiro elemento que atende a condição	javascript metodo array find

Como verificar se elemento existe no array?	array.includes(elemento) - retorna true/false	javascript metodo array includes

Como adicionar elemento no final do array?	array.push(elemento) - modifica array original	javascript metodo array push

Como remover último elemento do array?	array.pop() - remove e retorna o último elemento	javascript metodo array pop
```

## 🎨 CSS - Layout Básico

```
Como centralizar texto horizontalmente?	text-align: center;	css propriedade texto alinhamento

Como centralizar uma div horizontalmente?	margin: 0 auto; (div deve ter largura definida)	css propriedade layout centralizacao

Qual propriedade ativa o flexbox?	display: flex;	css propriedade flexbox layout

No flexbox, como alinhar itens horizontalmente?	justify-content (valores: center, flex-start, flex-end, space-between)	css propriedade flexbox alinhamento

No flexbox, como alinhar itens verticalmente?	align-items (valores: center, flex-start, flex-end, stretch)	css propriedade flexbox alinhamento

Como fazer flexbox quebrar linha?	flex-wrap: wrap;	css propriedade flexbox wrap

Como centralizar completamente com flexbox?	display: flex; justify-content: center; align-items: center;	css exemplo flexbox centralizacao
```

## 📐 CSS - Grid Básico

```
Qual propriedade ativa o CSS Grid?	display: grid;	css propriedade grid layout

Como definir 3 colunas iguais no grid?	grid-template-columns: 1fr 1fr 1fr; ou repeat(3, 1fr)	css propriedade grid colunas

Como definir espaçamento entre itens do grid?	gap: 10px; ou row-gap e column-gap	css propriedade grid spacing

Como fazer item ocupar 2 colunas no grid?	grid-column: span 2;	css propriedade grid span

Como fazer item ocupar 2 linhas no grid?	grid-row: span 2;	css propriedade grid span

Como centralizar todos os itens do grid?	place-items: center;	css propriedade grid alinhamento
```

## ⚛️ React - Hooks Básicos

```
Como criar um estado local no React?	const [estado, setEstado] = useState(valorInicial);	react hook useState sintaxe

Como executar código após o componente renderizar?	useEffect(() => { /* código */ }, []);	react hook useEffect sintaxe

Como fazer useEffect executar apenas uma vez?	useEffect(() => { /* código */ }, []); - array de dependências vazio	react hook useEffect mounting

Como fazer useEffect executar quando estado mudar?	useEffect(() => { /* código */ }, [estado]);	react hook useEffect dependencias

Como criar um efeito de limpeza no useEffect?	useEffect(() => { return () => { /* limpeza */ }; }, []);	react hook useEffect cleanup
```

## 🧩 React - Componentes

```
Como passar dados do pai para filho no React?	Via props: <ComponenteFilho prop={valor} />	react conceito props componentes

Como criar um componente funcional básico?	const NomeComponente = () => { return <div>conteúdo</div>; };	react sintaxe componente funcional

Como renderizar condicionalmente no React?	{condicao && <Componente />} ou {condicao ? <A /> : <B />}	react sintaxe renderizacao condicional

Como renderizar uma lista no React?	{array.map(item => <li key={item.id}>{item.nome}</li>)}	react sintaxe renderizacao lista

Por que usar key em listas do React?	Ajuda o React a identificar quais itens mudaram, melhorando performance na re-renderização	react conceito key lista performance
```

## 🌐 HTML - Tags Essenciais

```
Qual tag representa o cabeçalho principal da página?	<header>	html tag semantica header

Qual tag representa o conteúdo principal da página?	<main>	html tag semantica main

Qual tag representa o rodapé da página?	<footer>	html tag semantica footer

Como criar um link que abre em nova aba?	<a href="url" target="_blank">texto</a>	html atributo link target

Qual atributo torna um campo obrigatório?	required	html atributo form required

Como criar uma imagem com texto alternativo?	<img src="caminho" alt="descrição">	html tag img alt atributo
```

---

## 📝 Instruções para Importar

### Método 1: Arquivo de Texto
1. Copie o conteúdo entre ``` de cada seção
2. Salve como arquivo .txt (ex: frontend-basico.txt)
3. No Anki: File → Import
4. Selecione o arquivo
5. Configure:
   - Field separator: Tab
   - Field 1: Front
   - Field 2: Back  
   - Field 3: Tags

### Método 2: Colar Direto
1. No Anki: File → Import
2. Cole o texto diretamente
3. Configure os campos conforme acima

### ⚠️ Importante
- Cada linha = 1 card
- Campos separados por TAB
- Tags separadas por espaço
- Teste com 2-3 cards primeiro!
